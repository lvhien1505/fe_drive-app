import Layout from 'components/layout';

const ScreenMyDrive = () => {
    return (
        <Layout
            config={{
                path: '/drive/my-drive',
                text: 'Drive của tôi',
            }}
        >
            a
        </Layout>
    );
};

export default ScreenMyDrive;
