import Layout from 'components/layout';

const ScreenSharedWithMe = () => {
    return (
        <Layout
            config={{
                path: '/drive/recent',
                text: 'Gần đây',
            }}
        >
            a
        </Layout>
    );
};

export default ScreenSharedWithMe;
